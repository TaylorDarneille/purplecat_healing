import React from 'react';

function Nav() {
  return (
    <nav>
      <a href="#">a link</a>
      <a href="#">a link</a>
      <a href="#">a link</a>
      <a href="#">a link</a>
    </nav>

  );
}

export default Nav;